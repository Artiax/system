apiVersion: "kustomize.toolkit.fluxcd.io/v1"
kind: "Kustomization"
metadata:
  name: "cluster-replica"
  namespace: "controller-system"

spec:
  interval: "10m0s"

  sourceRef:
    kind: "GitRepository"
    name: "clusters"

  path: "${name}/${replica}"

  postBuild:
    substituteFrom:
    - kind: "ConfigMap"
      name: "metadata"
      optional: false

  prune: true